<!DOCTYPE html>
<html lang="no">
    <%- include('partials/head.ejs') %>
<body>
    
    <%- include('partials/navbar.ejs') %>
    
    <h1>Hvilken rev er søtest?</h1>
    
    <% if (locals.error) { %>
        <div class="error">
            <p><%= error %></p>
            <button onclick="location.reload()">Prøv igjen</button>
        </div>
    <% } else { %>
        <div class="fox-container">
            <div class="fox-card">
                <img src="<%= fox1 %>" alt="Rev 1" class="fox-image">
                <button class="vote-button" data-image="<%= fox1 %>">Denne reven er søtest!</button>
            </div>
            
            <div class="fox-card">
                <img src="<%= fox2 %>" alt="Rev 2" class="fox-image">
                <button class="vote-button" data-image="<%= fox2 %>">Denne reven er søtest!</button>
            </div>
        </div>
        
        <div id="feedback">
            <p>Takk for din stemme! Du har valgt den søteste reven.</p>
            <button class="new-vote" onclick="location.reload()">Se flere rever</button>
        </div>
        
        <!-- Add this section for statistics -->
        <div id="statistics" class="statistics">
            <h2>Topp 5 søteste rever</h2>
            <div id="top-foxes" class="top-foxes-container">
                <!-- Top foxes will be displayed here -->
            </div>
        </div>
        
        <!-- Toast notification -->
        <div id="toast" class="toast">
            <!-- Leader message will appear here -->
        </div>
        
        <!-- Add this below the statistics section but before the script tag -->
        <div class="leaderboard-link">
            <a href="/leaderboard" class="leaderboard-button">Se fullstendig rangering av alle rever</a>
        </div>
    <% } %>
    
    <script src="/js/voting.js"></script>
</body>
</html>